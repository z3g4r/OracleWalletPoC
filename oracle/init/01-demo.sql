ALTER SESSION SET CONTAINER=FREEPDB1;

BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE app_user.wallet_login_demo (id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, message VARCHAR2(200), created_at TIMESTAMP DEFAULT SYSTIMESTAMP)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -955 THEN
      RAISE;
    END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE q'[INSERT INTO app_user.wallet_login_demo (message) VALUES ('wallet login demo row')]';
  COMMIT;
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -1 THEN
      RAISE;
    END IF;
END;
/
